// STUB — the original src/vighnaa.js was never committed to the repo, and no
// machine with a copy could be found, so deploys from a fresh clone failed on
// the import in worker.js. Returning null makes the /vighnaatextllm route fall
// through to static assets. If the original file turns up, replace this stub
// with it and everything reconnects — worker.js is unchanged.
export async function handleVighnaa(request, env, url) {
  return null;
}
